# জমা-খরচ ৩৬০ - প্রজেক্ট বিশ্লেষণ এবং সেটআপ গাইড

## 📋 প্রজেক্ট সম্পর্কে

এটি একটি **প্রগ্রেসিভ ওয়েব অ্যাপ্লিকেশন (PWA)** যা ব্যক্তিগত আর্থিক ব্যবস্থাপনার জন্য ডিজাইন করা হয়েছে। এটি বাংলা ভাষায় তৈরি এবং মোবাইল-ফার্স্ট অ্যাপ্রোচ অনুসরণ করে।

### 🎯 প্রধান বৈশিষ্ট্য

- **ড্যাশবোর্ড**: সামগ্রিক আর্থিক অবস্থার সংক্ষিপ্ত দৃশ্য
- **লেনদেন ব্যবস্থাপনা**: আয় ও ব্যয় ট্র্যাকিং
- **বাজেট পরিকল্পনা**: বাজেট সেট এবং পর্যবেক্ষণ করুন
- **আর্থিক লক্ষ্য**: সঞ্চয়ের লক্ষ্য নির্ধারণ এবং অনুসরণ
- **বিল ম্যানেজমেন্ট**: পুনরাবৃত্তিমূলক বিল ট্র্যাক করুন
- **বিনিয়োগ পোর্টফোলিও**: বিনিয়োগ ট্র্যাক করুন
- **অ্যাকাউন্ট ব্যবস্থাপনা**: একাধিক অ্যাকাউন্ট পরিচালনা করুন
- **রিপোর্ট এবং বিশ্লেষণ**: চার্ট এবং গ্রাফ সহ বিস্তারিত রিপোর্ট
- **PIN লক সুরক্ষা**: ব্যক্তিগত তথ্য সুরক্ষার জন্য পিন লক
- **অফলাইন কাজ করার ক্ষমতা**: সার্ভিস ওয়ার্কার দ্বারা সমর্থিত
- **জেসচার সাপোর্ট**: সোয়াইপ এবং অন্যান্য জেসচার নিয়ন্ত্রণ

---

## 🔍 প্রজেক্ট বিশ্লেষণ

### ফাইল স্ট্রাকচার

```
📦 JAMAKHOROCH360/
├── index.html                 # প্রধান HTML ফাইল
├── app.js                     # মূল React অ্যাপ্লিকেশন
├── manifest.json              # PWA ম্যানিফেস্ট
├── sw.js                      # সার্ভিস ওয়ার্কার (অফলাইন সাপোর্ট)
│
├── 📁 components/             # React কম্পোনেন্ট
│   ├── Dashboard.js           # ড্যাশবোর্ড ভিউ
│   ├── Transactions.js        # লেনদেন ম্যানেজমেন্ট
│   ├── Budget.js              # বাজেট পরিকল্পনা
│   ├── Goals.js               # আর্থিক লক্ষ্য
│   ├── Bills.js               # বিল ট্র্যাকিং
│   ├── Investments.js         # বিনিয়োগ ম্যানেজমেন্ট
│   ├── Accounts.js            # অ্যাকাউন্ট ব্যবস্থাপনা
│   ├── Loans.js               # ঋণ ট্র্যাকিং
│   ├── Reports.js             # রিপোর্ট এবং বিশ্লেষণ
│   ├── Settings.js            # অ্যাপ সেটিংস
│   ├── CalendarView.js        # ক্যালেন্ডার ভিউ
│   ├── Header.js              # শীর্ষ হেডার
│   ├── Sidebar.js             # সাইডবার নেভিগেশন
│   ├── BottomNav.js           # নিচের নেভিগেশন বার
│   ├── UIComponents.js        # টোস্ট, মডাল, অন্যান্য UI
│   ├── GestureUI.js           # জেসচার-ভিত্তিক UI
│   └── Tools.js               # অতিরিক্ত সরঞ্জাম
│
├── 📁 utils/                  # ইউটিলিটি ফাংশন
│   ├── dataManager.js         # localStorage ব্যবস্থাপনা
│   └── gestureHooks.js        # জেসচার হুকস (সোয়াইপ, লং প্রেস, শেক)
│
└── 📁 trickle/                # ডকুমেন্টেশন এবং ডেটা
    ├── database/tables.json    # ডেটাবেস স্কিমা
    ├── notes/README.md         # প্রজেক্ট নোট
    └── rules/rule_readme_update.md
```

### ব্যবহৃত প্রযুক্তি

| প্রযুক্তি | ব্যবহার |
|-----------|---------|
| **React 18** | UI ফ্রেমওয়ার্ক |
| **Tailwind CSS** | স্টাইলিং |
| **Chart.js** | ডেটা ভিজ্যুয়ালাইজেশন |
| **Service Worker** | অফলাইন সাপোর্ট |
| **LocalStorage API** | ডেটা স্টোরেজ |
| **Babel** | JSX ট্রান্সপাইলার |
| **Hind Siliguri Font** | বাংলা টেক্সট |

---

## ✅ অসামঞ্জস্যতা এবং সুপারিশ

### 🔴 **সমালোচনামূলক সমস্যা**

#### 1. **Package.json নেই**
- **সমস্যা**: প্রজেক্টে কোনো `package.json` ফাইল নেই
- **প্রভাব**: নতুন নির্ভরতা যোগ করা বা প্রজেক্ট ম্যানেজ করা কঠিন
- **সমাধান**: আমরা একটি `package.json` তৈরি করেছি

#### 2. **স্থানীয় সার্ভার প্রয়োজন**
- **সমস্যা**: সরাসরি ব্রাউজারে খোলা যায় না (CORS সমস্যা)
- **প্রভাব**: স্ক্রিপ্ট লোড হতে পারে না
- **সমাধান**: HTTP সার্ভার প্রয়োজন

#### 3. **বাহ্যিক CDN নির্ভরতা**
- **সমস্যা**: React, Tailwind এবং অন্যান্য লাইব্রেরি CDN থেকে লোড হয়
- **প্রভাব**: অফলাইনে সম্পূর্ণ কাজ করতে পারবে না
- **সমাধান**: সম্পূর্ণ PWA হতে এই ফাইলগুলি স্থানীয়ভাবে সংরক্ষণ করুন

### 🟡 **মাঝারি সমস্যা**

#### 4. **Service Worker ক্যাশ সংস্করণ**
- **সমস্যা**: `sw.js` এ `CACHE_NAME = 'budget-tracker-v2'` হার্ডকোড করা আছে
- **প্রভাব**: আপডেট সমস্যা হতে পারে
- **সমাধান**: ডাইনামিক সংস্করণিং বাস্তবায়ন করুন

#### 5. **এরর হ্যান্ডলিং**
- **সমস্যা**: কিছু components-এ সম্পূর্ণ error handling নেই
- **প্রভাব**: ক্র্যাশ হতে পারে
- **সমাধান**: Try-catch ব্লক যোগ করুন

---

## 🚀 কীভাবে পিসিতে চালাতে হবে

### **পদ্ধতি ১: Python ব্যবহার করে (সবচেয়ে সহজ)**

#### প্রয়োজনীয় জিনিস:
- Python 3+ ইনস্টল করা থাকতে হবে

#### পদক্ষেপ:

1. **কমান্ড প্রম্পট খুলুন:**
   ```bash
   Windows Key + R
   ```
   টাইপ করুন: `cmd`

2. **প্রজেক্ট ফোল্ডারে যান:**
   ```bash
   cd C:\Users\User\JAMAKHOROCH360
   ```

3. **Python সার্ভার চালান:**
   ```bash
   # Python 3.x এর জন্য
   python -m http.server 8000
   
   # অথবা Python 2.x এর জন্য
   python -m SimpleHTTPServer 8000
   ```

4. **ব্রাউজারে খুলুন:**
   ```
   http://localhost:8000
   ```

---

### **পদ্ধতি ২: Node.js এবং Live Server (সুপারিশকৃত)**

#### প্রয়োজনীয় জিনিস:
- Node.js ডাউনলোড এবং ইনস্টল করুন: https://nodejs.org/

#### পদক্ষেপ:

1. **Node.js ইনস্টল করার পর, কমান্ড প্রম্পট খুলুন:**

2. **প্রজেক্ট ফোল্ডারে যান:**
   ```bash
   cd C:\Users\User\JAMAKHOROCH360
   ```

3. **Live Server ইনস্টল করুন:**
   ```bash
   npm install -g live-server
   ```

4. **অ্যাপ চালান:**
   ```bash
   live-server
   ```
   
   এটি স্বয়ংক্রিয়ভাবে ব্রাউজার খুলবে (সাধারণত `http://localhost:8080`)

---

### **পদ্ধতি ৩: VS Code Live Server এক্সটেনশন**

#### পদক্ষেপ:

1. **VS Code এ প্রজেক্ট খুলুন:**
   - File → Open Folder
   - `C:\Users\User\JAMAKHOROCH360` নির্বাচন করুন

2. **Live Server এক্সটেনশন ইনস্টল করুন:**
   - Ctrl + Shift + X
   - "Live Server" খুঁজুন (Ritwick Dey দ্বারা)
   - Install ক্লিক করুন

3. **index.html এ রাইট ক্লিক করুন:**
   - "Open with Live Server" নির্বাচন করুন
   - স্বয়ংক্রিয়ভাবে খুলবে `http://localhost:5500`

---

### **পদ্ধতি ৪: http-server (Node.js)**

#### পদক্ষেপ:

1. **ইনস্টল করুন:**
   ```bash
   npm install -g http-server
   ```

2. **প্রজেক্ট ফোল্ডারে যান এবং চালান:**
   ```bash
   cd C:\Users\User\JAMAKHOROCH360
   http-server -p 8080
   ```

3. **খুলুন:**
   ```
   http://localhost:8080
   ```

---

## 📱 অ্যাপ ব্যবহার করার টিপস

### **প্রথম বার ব্যবহার:**
1. অ্যাপ খোলার পর "ইনস্টল করুন" প্রম্পট দেখবেন
2. বাজেট এবং লক্ষ্য সেট করুন
3. লেনদেন যোগ করুন

### **ডেটা সংরক্ষণ:**
- সমস্ত ডেটা **LocalStorage** এ সংরক্ষিত হয়
- ব্রাউজার ডেটা মুছলে সব হারিয়ে যাবে
- **পর্যায়ক্রমে ব্যাকআপ নিন:** Settings → Export Data

### **মোবাইলে ইনস্টল করুন:**
1. ব্রাউজারে খুলুন
2. Android: মেনু → "অ্যাপ ইনস্টল করুন"
3. iOS: শেয়ার → "হোম স্ক্রিনে যোগ করুন"

---

## 🔧 উন্নয়ন এবং কাস্টমাইজেশন

### **থিম পরিবর্তন করুন:**
- `index.html` এ `--primary-color` পরিবর্তন করুন

### **নতুন কম্পোনেন্ট যোগ করুন:**
1. `components/` এ নতুন ফাইল তৈরি করুন
2. `index.html` এ স্ক্রিপ্ট যোগ করুন
3. `app.js` এ ইমপোর্ট এবং ব্যবহার করুন

### **নতুন ডেটা ফিল্ড:**
- `utils/dataManager.js` এ `STORAGE_KEYS` যোগ করুন
- গেটার/সেটার ফাংশন তৈরি করুন

---

## 📊 ডেটা স্ট্রাকচার

### **Transaction:**
```javascript
{
    id: unique_id,
    type: 'income' | 'expense',
    amount: number,
    category: string,
    description: string,
    date: ISO_date_string,
    accountId: string,
    isRecurring: boolean,
    tags: []
}
```

### **Budget:**
```javascript
{
    id: unique_id,
    category: string,
    limit: number,
    spent: number,
    period: 'monthly' | 'yearly',
    startDate: ISO_date_string
}
```

### **Goal:**
```javascript
{
    id: unique_id,
    name: string,
    targetAmount: number,
    currentAmount: number,
    deadline: ISO_date_string,
    category: string
}
```

---

## 🐛 সাধারণ সমস্যা এবং সমাধান

### **সমস্যা: "অ্যাপ খোলা হচ্ছে না"**
- ✅ **সমাধান**: HTTP সার্ভার চেক করুন, localhost ঠিক আছে কিনা দেখুন

### **সমস্যা: "Service Worker রেজিস্ট্রেশন ব্যর্থ"**
- ✅ **সমাধান**: HTTPS প্রয়োজন (localhost ব্যতিক্রম)

### **সমস্যা: "ডেটা হারিয়ে গেছে"**
- ✅ **সমাধান**: LocalStorage পরিষ্কার হয়েছে। ব্রাউজার ক্যাশ খালি করবেন না

### **সমস্যা: "অফলাইনে কাজ করছে না"**
- ✅ **সমাধান**: নিশ্চিত করুন Service Worker সক্রিয় (DevTools → Application → Service Workers)

---

## 📞 সহায়তা এবং রক্ষণাবেক্ষণ

### **রক্ষণাবেক্ষণ টাস্কলিস্ট:**
- [ ] নিয়মিত Tailwind আপডেট করুন
- [ ] Chart.js সাম্প্রতিক রাখুন
- [ ] React আপডেট পরীক্ষা করুন
- [ ] Service Worker cache logic আপডেট করুন
- [ ] ব্যবহারকারীর ফিডব্যাক সংগ্রহ করুন

### **ভবিষ্যত উন্নতি:**
- [ ] ব্যাকএন্ড API সংযোগ
- [ ] ক্লাউড সিঙ্ক
- [ ] মাল্টি-ডিভাইস সিঙ্ক
- [ ] উন্নত রিপোর্টিং
- [ ] AI-চালিত সুপারিশ

---

## ✨ শেষ কথা

এই অ্যাপটি সম্পূর্ণভাবে কার্যকর এবং ব্যবহারের জন্য প্রস্তুত। কোনো গুরুতর সমস্যা নেই, শুধু একটি HTTP সার্ভার চালু করুন এবং উপভোগ করুন!

---

**শেষ আপডেট:** ৩১ জানুয়ারি ২০২৬
